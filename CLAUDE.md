# Jany 基金看板 · AI 协作入口

> **这是索引，不是全文。** 详细规范在 `docs/`，本文件只保留：读什么、红线、以及"怎么不丢信息"。
> 收到任何需求：**先按下方顺序读文档 → 再读相关源文件 → 再动手**。

---

## 一、读这些（按需，不必全读）

| 文档 | 什么时候必须读 |
| --- | --- |
| [docs/01-业务意图.md](docs/01-业务意图.md) | 碰计算、信号、字段语义前。判断"这段代码是不是有意设计"的唯一依据 |
| [docs/02-系统架构.md](docs/02-系统架构.md) | 碰分层、数据流、网关、云同步、夜间引擎前 |
| [docs/03-实现约束.md](docs/03-实现约束.md) | 碰任何代码前 |
| [docs/04-开发纪律.md](docs/04-开发纪律.md) | 提交前对照检查清单 |
| [docs/05-未实现项.md](docs/05-未实现项.md) | 觉得"这里少了个功能"时先查，多半是刻意不做 |
| [docs/DECISIONS.md](docs/DECISIONS.md) | **改动或删除任何既有机制前必读**，见第三节 |

投资方法论要点已自包含在 `01-业务意图.md`，无需外部方法论文档即可理解看板。
方法论更新时由用户提供新文件，据此回头修订该篇。

---

## 二、红线（违反即打回，不必讨论）

1. **数据源走 `DATA_MODE` 双模，禁止第三处另开**：`config.js` 的 `DATA_MODE` 切换两条链路——
   - `"gateway"`：走 `API_BASE` 三个端点，主备切换 + KV last-known-good 全在 `workers/fund-market-api/` 网关内。
   - `"direct"`：浏览器直连腾讯行情 `TX_BASE`，单源、零跨境延迟，最快但无主备；字段解析在 `js/data.js` 的 `_fetchTencentFunds` / `_fetchIndexGroupTencent`。
   改数据源逻辑只在这两套分支内，不得到处加 fetch。
2. **行情数据禁止因单次失败清空**：拿不到新数据时退回上次好数据并标记为陈旧，绝不把已有数据冲成空。
   保护落在**网关侧**（对所有设备生效），不是浏览器 localStorage。详见 [D-001](docs/DECISIONS.md)。
3. **`js/` 文件上限 10 个**，已满。要加功能先合并，不许新增。
4. **业务数字只在 `config.js`**，不许散落。
5. **配置写入必经 store 封装**，每个封装自己调 `bumpConfigVer()`，不许依赖调用方顺带调了别的。
6. **净值判据三处逐字一致**：`f.offVal && (!estD || offD >= estD)`。
7. **engine 是纯函数**，外部依赖全部参数传入。
8. **改后代码量不许增加**：改根源不包裹，删死变量僵尸函数。
9. **不擅自补功能**：审查只报问题。觉得缺功能 → 记录，不动手。
10. **结构性改动先问**：目录、文件清单、跨层方案、删疑似有用的函数。

---

## 三、防信息丢失机制（这套的存在理由，务必遵守）

**已经发生过的事故**：2026-06-19 建立的"行情数据 last-known-good 保护"只活在代码里，从未写进文档；
2026-07-20 做网关迁移时，AI 读了文档里"整组失败即不可用"的表述，照着执行，把保护拆了，
导致收盘后一次刷新把全天数据冲成空白。**根因不是记性差，是判断依据没有被固化。**

因此立下三条：

### 3.1 决策日志只增不改

`docs/DECISIONS.md` 是**追加式**的。**任何"删除、替换、绕过既有机制"的改动，先在其中追加条目，再动代码。**
条目写清：背景、决策、**理由**、代价、代码位置、取代了哪条。
理由那栏是重点——代码能表达"做了什么"，表达不了"为什么这么做、为什么不那么做"。

### 3.2 文档不许原地删

某节被整体取代时，把旧版移进 `docs/archive/<原文件名>-<日期>.md`，不要直接删。
`docs/archive/` 是给未来的 AI 看的考古层，不参与日常阅读。

### 3.3 代码里的反直觉设计必须双写

凡是"看起来像 bug、其实是有意为之"的地方（如 `totalFriction:0`、`xqNav` 口径、`getDynamicTarget` 边界兜底），
代码注释和 `docs/03-实现约束.md` 两边都要有，且注明**为什么**。只写一边等于没写。

> **给 AI 的话**：你读到的文档可能不完整，代码里可能有文档没记的设计。
> **发现代码与文档冲突时，默认代码是对的、文档是漏的**——先问，不要按文档去"修正"代码。
> 上面那起事故就是反着来造成的。

---

## 四、项目速览

纯原生前端，零构建，零框架，唯一外部库 CDN 的 `Sortable.js`。

```
fund-monitor/
├── CLAUDE.md                 # 本文件（索引）
├── docs/                     # 规范全文 + 决策日志 + 归档
├── index.html · favicon.png · CNAME
├── css/style.css
├── js/                       # 前端运行时，10 个文件，已满
│   config · logger · store · data · engine · ui · ui-holding · ui-pe · interact · main
├── workers/
│   ├── fund-market-api/      # 市场数据网关（Cloudflare Pages Functions）
│   └── pe-night-trigger/     # 夜间引擎触发器（Cloudflare Worker）
└── automation/               # 夜间 PE 数据引擎（Python + GitHub Actions）
```

**数据源双模（`DATA_MODE`，详见 `docs/DECISIONS.md` D-013）**：
`config.js` 一个常量控制全站走网关还是直连腾讯。`"direct"` 时浏览器直连 `qt.gtimg.cn`，
单请求覆盖「官方净值+盘中估算」两链、指数单独单请求，返回 GBK 须 `TextDecoder("gbk")` 解码；
`workers/` 网关此时不被前端使用，仅作为备选保留。`"gateway"` 为当前默认。

加载顺序：`config → logger → store → data → engine → ui → ui-holding → ui-pe → interact → main`

分层依赖单向向下，下层绝不调上层：

```
config → store → data ─┐
                        ├→ ui → interact → main
              engine ──┘
```

**十文件上限只约束 `js/`**，`workers/` 与 `automation/` 不占配额。

> **结构备注**：以上为 GitHub 仓库的真实结构。Claude 项目知识库受限只能平铺存放副本，
> 那只是托管形式，不代表真实目录——勿据平铺副本"修正"本结构。
