# 决策日志（DECISIONS）

> **追加式，只增不改。** 新条目加在最前面。已有条目**永不删除、永不改写**——
> 决策被推翻时，写一条新的并在新条目里注明"取代 D-xxx"，同时把旧条目状态改为 `已被取代`（仅改这一行）。
>
> **什么时候必须写**：删除、替换、绕过任何既有机制之前。**先写这里，再动代码。**
>
> **什么时候不必写**：判据是**理由能否从代码本身复原**。能 → 只写代码注释，不占条目。
> 条目越滥，索引表越不可信，跳读就失效了——那才是本文件真正会死的方式，不是行数变大。
>
> **条目模板**：背景 / 决策 / 理由 / 代价 / 代码位置 / **须同步修订的正文章节** / 取代关系。
> 后两栏可以填「无」，但**不能省略**——省略等于没检查过。「须同步修订的正文章节」是 2026-07-21
> 新增的必填栏，专为堵住"决策写了、正文没跟上"这个缺口（缘由见 [D-016](#d-016)）。
>
> **为什么存在**：代码能表达"做了什么"，表达不了"为什么这么做、为什么当初否决了另一种做法"。
> 后者一旦只存在于某次对话里，对话过期就永久丢失，下一个人（或下一个 AI）会理直气壮地拆掉它。
> 本项目已经因此出过一次事故，见 D-001。
>
> **超过约 800 行时分卷**：把状态为 `已被取代 / 已失效` 的条目整体移入
> `docs/archive/DECISIONS-<年份>.md`，索引表保留该行并注明去向。**迁移不是删除**，
> 与 CLAUDE.md §3.2 的正文归档同一套逻辑，只增不改完好无损。

## 索引

| 编号 | 日期 | 决策 | 状态 |
| --- | --- | --- | --- |
| [D-022](#d-022) | 2026-07-21 | 基准代理改为入库前统一回填，权益%/总额/收益共用一条净值链 | 生效中 |
| [D-021](#d-021) | 2026-07-21 | 解除 `js/` 十文件上限；红线 #6/#8/#9 补界定 | 生效中 |
| [D-020](#d-020) | 2026-07-21 | 新增第4数据组「业绩基准代理」并回填估算列；净值加主备（指数仍单源）；标签显示源名称且主源出声 | 生效中 |
| [D-019](#d-019) | 2026-07-21 | 收益口径加日期闸门，「今日收益」更名「最新收益」；红线 #6 改为两类口径 | 生效中 |
| [D-018](#d-018) | 2026-07-21 | 估算缓存按数据自带时间戳标记陈旧；Gist 推送时机与读侧节流修正 | 生效中 |
| [D-017](#d-017) | 2026-07-21 | PE 锚定路径由 `PE_ANCHOR` 统一，持仓抽屉与 PE 栏对齐 | 生效中 |
| [D-016](#d-016) | 2026-07-21 | 追认：实时 PE 改由旁路引擎百分位提供；决策条目增「文档影响」栏 | 生效中 |
| [D-015](#d-015) | 2026-07-21 | 估算数据源（天天基金/腾讯）持续失效，暂不处理，网关验收作废归档 | 观察中 |
| [D-014](#d-014) | 2026-07-20 | 估算数据持久化（localStorage + Gist 三层回退） | 生效中 |
| [D-013](#d-013) | 2026-07-20 | 数据源双模：`DATA_MODE` 切换网关 / 腾讯直连 | 生效中 |
| [D-012](#d-012) | 2026-07-20 | 盘后跳过估算请求，节省 4s+ 刷新等待 | 生效中（直连模式除外，见 D-014） |
| [D-011](#d-011) | 2026-07-20 | 网关 in-flight 请求去重 | 生效中 |
| [D-010](#d-010) | 2026-07-20 | "已更新"徽标与净值选取判据拆开 | 生效中 |
| [D-009](#d-009) | 2026-07-19 | 上游请求一律带缓存击穿参数 | 生效中 |
| D-008 | 2026-07-16 | 前端一律不直连第三方行情，全部收口到网关 | 已被取代（由 D-013）·**已归档** → [archive/DECISIONS-2026.md](archive/DECISIONS-2026.md#d-008) |
| [D-007](#d-007) | 2026-07-16 | 不再用 gitignore 藏"本地专属"文件 | 生效中 |
| [D-006](#d-006) | 2026-07-19 | 拆除 PE 双路验证层，保留 2.0 展示 | 生效中 |
| [D-005](#d-005) | 2026-06 | 配置同步用版本号"谁新谁赢 + 反向自愈" | 生效中 |
| [D-004](#d-004) | 早于 2026-06 | 增权预案 `totalFriction` 恒为 0 | 生效中 |
| [D-003](#d-003) | 早于 2026-06 | 增权份额除以兴全净值而非买入标的净值 | 生效中 |
| [D-002](#d-002) | 2026-07-16 | 网关"整组成败"，禁止主备混用 | 生效中（边界见 D-001） |
| [D-001](#d-001) | 2026-06-19 / 2026-07-20 | 行情数据 last-known-good 保护，落在网关侧 | 生效中 |

---

<a id="d-022"></a>
## D-022 · 基准代理改为入库前统一回填，权益% / 持仓总额 / 收益共用一条净值链

**状态**：生效中
**日期**：2026-07-21
**关联**：[D-020](#d-020)（代理引入，本条修订其 D 节）/[D-019](#d-019)（两类口径）/[D-010](#d-010)（徽标与取值拆分）

### 背景

用户观察：**「今天大盘涨了很多，权益百分比却几乎没变化。」**

查证属实且必然如此。`calcCurrentEquity` = `Σ(份额 × 净值 × 权益系数) / Σ(份额 × 净值)`，
权益系数是 `PRODUCTS` 里的静态值，唯一变量是净值；净值取自 `getNavByCode`（市值口径），
优先用官方净值，而官方净值当日 19:30 前恒为昨日值。**分子分母全是昨天的数，
数学上不可能随大盘变动。**

根因是 D-020 的一个刻意选择：代理回填只作用于**渲染层副本**（`UI_updateFunds` 里的
`displayResults`），store 里的 `results` 保持"纯净"。当时顾虑是代理值会沉进估算缓存、
被推去 Gist 冒充真估算。结果就是同一屏上两套数字：顶部收益走代理（会动），
权益%/持仓总额走 `getNavByCode` 读 store（不会动）。

### 决策

**A. 代理回填上移到入库前**

- `refreshData` 在 `setLastResults` **之前**调 `applyProxyEstimates`，results 从此只有一条净值链。
- 全站（权益%、持仓总额、最新收益、卡片估算列、增权/降权预案）读同一份数字。
- 渲染层不再另做副本。

**B. 顾虑经复查不成立**

- 估算缓存由 `saveEstCache(r.estimate)` 写入，数据来自 `fetchEstimates` 抓到的**原始 map**；
  `pushEstToGist` 读 `loadEstCacheEntry()`。**两者都不读 `results`。**
- 故代理值进 `results` 不会沉进缓存、不会被推去 Gist。原顾虑针对的是"就地改 results 对象"，
  而实现始终是返回新对象——两件事被混为一谈了。

**C. 官方净值落地时一只一只替换，不需要额外代码**

- `getNavByCode` 的既有判据 `f.offVal && (!estD || offD >= estD)` 已经实现了这个语义：
  官方日期不旧于估算日期就用官方。晚间某只官方净值出来（`offD` 变今日），该只自动切官方，
  其余仍用代理。**"一个替换一个"是既有判据的自然结果，本条只是让它终于有数据可判。**

**D. 代理分支从两处收益计算中删除**

- `calcTodayProfit` 与 `interact.js` `profitMap` 里各有一段代理兜底（D-020 加的），
  上移后 `estD` 必为今日，这两段**永不进入**，属死代码，按红线 #8 删除。
- 连带删除 `calcTodayProfit` / `renderTodayProfit` 的 `indicesMap` 参数。
- **红线 #6 的同步契约因此回到"只有一套判据"**，比 D-020 之后更简单。

**E. 「已更新」徽标不认代理**

- 徽标判据从 `isFundUpdated` 拆出独立的 `isRealUpdate = isOffToday || (isEstToday && estSource !== "proxy")`。
- 若沿用 `isFundUpdated`，估算源全死的日子里徽标会天天亮着——一条真数据都没到却说"已更新"，
  等于永久说谎。**D-010 当初拆开徽标与取值判据，理由正是"数值可放宽、状态必须严格"，
  本条是那条原则的再次应用。**

**F. 指数 tick 触发代理重算**

- `INDICES` 频道调 `reapplyProxyEstimates()`：重算代理 → `setLastResults` → 广播 FUNDS → 重画。
- 不接的话权益%只能等 60 秒的净值刷新才动一次，而指数是 10 秒一跳。
- `applyProxyEstimates` 为此改为**幂等**：跳过条件由"当日已有估算"改为"当日已有**真**估算"
  （`estSource !== "proxy"`），否则自产的代理条目会被自己挡住、指数再涨也不更新。
- 这条线同时是 D-020 B' 那个冷启动竞态的解药，故 B' 的 `UI_updateFunds` 直连可以撤掉。

### 理由

- **一份数据一个来源**：同屏两套数字是 bug 温床，且这次的表现（顶部动、权益%不动）
  用户一眼就看出来了，说明它违反的是最朴素的直觉预期。
- **为什么当初的"纯净 results"是错的抽象**：results 不是"外部数据的原始记录"，
  它本来就是 official + estimate 合并出来的**派生视图**（`fetchSingleFund` 做的就是合并）。
  往里加一个派生字段并不破坏什么；真正需要保持纯净的是**持久化层**（估算缓存 / Gist），
  而那一层根本不读 results。**边界画错了地方，导致为了守一个不存在的不变量，
  制造了一个真实的不一致。**
- **精度取舍**：代理不含经理 alpha，纯债那条挂国债指数只是方向占位。
  但权益%本就是**观察与决策辅助**，且申赎按当日收盘净值成交，盘中精确到小数点后两位没有意义——
  知道"敞口大致漂到哪了"才是它的用途。官方净值当晚落地即自动校正。

### 代价

- 权益%、持仓总额、增权/降权预案的金额在盘中全部变成**估算值**。19:30 后随官方净值逐只校正。
  用户明确要求如此（"整个数据链是统一且相通的"）。
- 预案里的买入金额/卖出份额同样基于估算净值。**申赎按当日收盘净值成交，盘中任何算法都算不出
  精确份额**，故这不是新引入的误差，只是把误差来源从"昨日净值"换成了"今日估算"——后者更近。
- `js/` 净增从 209 → 230 行（+21）：新增 `reapplyProxyEstimates`（约 17 行），
  删掉两段代理兜底与三处 `indicesMap` 参数传递，抵掉大部分。
- 指数每 10 秒触发一次全量重画（此前 60 秒一次）。`renderCards`/`renderTable` 有
  `isStructureUnchanged` 原地打补丁，非整树重建；`reapplyProxyEstimates` 在无持仓/无结果时直接返回。

### 代码位置

- `js/interact.js`：`refreshData` 入库前回填；新增 `reapplyProxyEstimates`；`profitMap` 删代理分支
- `js/engine.js`：`applyProxyEstimates` 改幂等 + 注释重写；`calcTodayProfit` 删代理分支、删 `indicesMap` 参数、拆出 `isRealUpdate`
- `js/main.js`：`INDICES` 频道改调 `reapplyProxyEstimates`
- `js/ui.js`：`UI_updateFunds` 不再做渲染层副本；`renderTodayProfit` 删 `indicesMap` 参数
- `js/data.js`：**无改动**——`getNavByCode` 的判据原样生效，这正是本条能成立的原因

### 须同步修订的正文章节

- `CLAUDE.md` 红线 #6：删掉 D-020 加的"代理回退分支同样在同步契约内"，改为说明链路已在上游统一（已改）
- `docs/03-实现约束.md` §3.3：同上，并补市值口径现在吃代理估值的说明（已改）
- `docs/03-实现约束.md` §3.6：`renderTodayProfit` 签名回到 5 参（已改）
- `docs/02-系统架构.md` §2.1 数据流图：回填位置由渲染层移到 `setLastResults` 前（已改）

### 取代关系

**取代 [D-020](#d-020) 的 D 节**（回填发生在 ui 层、顶部收益另走 `calcTodayProfit` 内部代理分支）
与 **B' 节**（`INDICES` 频道直连 `UI_updateFunds`）。D-020 其余各节继续生效。

---

<a id="d-021"></a>
## D-021 · 解除 `js/` 十文件上限；红线 #6 / #8 / #9 补界定

**状态**：生效中
**日期**：2026-07-21
**关联**：[D-020](#d-020)（实施过程中暴露了这三处条文问题）

### 背景

做 D-020 时有三条红线在实际执行中卡住或被误读，用户逐条裁决：

1. **红线 #3「`js/` 上限 10 个，已满」** —— `applyProxyEstimates` 因「ui 层禁止业务计算」无处安放，
   受十文件上限所限也不能新开一层，一度打算给它开白名单例外。
2. **红线 #6「收益口径两处必须逐字同步」** —— D-020 给这两处各加了基准代理回退分支，
   但条文只列了原有的「官方 vs 估算」判据，没说新分支也在契约内。
3. **红线 #9「不擅自补功能」** —— 执行已批方案时发现方案漏了「盘中估算」列，
   条文字面是"记录，不动手"，但这属于方案自身的缺口而非 AI 自己起意，边界不清。
4. **红线 #8「改后代码量不许增加」** —— D-020 净增 249 行，条文字面即违反，
   但功能是用户批准的；初版风险表为绕过该条声称"总量持平"，属于虚报。

### 决策

- **解除 `js/` 文件数上限**（用户裁决）。新增文件仍需先问（红线 #10 不变），且必须落在既有分层。
  条文同时补一句反射：**先问"这个函数是不是本来就该属于某个既有层"**——
  `applyProxyEstimates` 是纯函数、本就属于 engine，放对地方后「ui 不该算业务」的冲突自动消失，
  根本不需要新文件，也不需要白名单例外。**本次因此没有新增任何文件，上限解除了但没用上。**
- **红线 #6 明确把基准代理回退分支纳入同步契约**，并注明它是后加的第三种取值来源、最易漏同步。
- **红线 #9 改为「不擅自补功能，但鼓励提议」**，界定为：约束的是 AI 自己起意加东西，不是闭嘴；
  执行已批方案时发现方案缺口属提议范畴，应提出等裁决，既不默默补也不默默跳过。
  用户原话：**"不擅自补，和提议我裁决，不冲突，且提议我更支持"。**
- **红线 #8 界定为反包裹式膨胀**，不是禁止一切行数增长：新增真实能力可以净增，
  但必须在决策条目里如实报数，不许虚报"持平"。附三条自查问句。

### 理由

- 十文件上限本是为逼迫"先合并再加功能"，防止文件碎片化。但它与「ui 层禁止业务计算」叠加后，
  会把代码逼进"要么放错层、要么开例外"的死角——**两条约束同时收紧，结果是逼人破坏其中一条**。
  解除数量限制、保留"新增先问"，既留住了防碎片的意图，又不再制造死角。
- 前三条都属同一类病：**条文只描述了立规时的世界**，世界一变（多了代理分支、多了已批方案的执行场景），
  字面执行就开始产生错误结果。补界定而非删条文，是因为原意都还成立，坏的只是覆盖面。
- 红线 #8 尤其危险：一条无法诚实满足的规则，会诱导执行者去伪造合规证据（初版那句"总量持平"）。
  **规则必须留出诚实的合规路径，否则它训练的是造假而不是克制。**

### 代价

- 文件数不再有硬闸，长期可能重新碎片化。缓解：红线 #10「新增先问」仍在，且新增了"先问是否属于既有层"的反射。
- 红线条文变长，索引性下降。判断：这三条都是高频踩中的，值得占篇幅。

### 代码位置

无代码改动，纯条文。

### 须同步修订的正文章节

- `CLAUDE.md` 第二节红线 #3 / #6 / #8 / #9（已改）
- `CLAUDE.md` 第四节「项目速览」中"十文件上限只约束 `js/`"一句（已改）
- `docs/03-实现约束.md` §3.3：收益口径表下补代理分支同步警告（已改）
- `docs/02-系统架构.md` §2.1：若有"js 十文件"表述需同步（复查确认无）

### 取代关系

不取代整条决策，仅修订 `CLAUDE.md` 红线 #3 / #6 / #8 / #9 的表述。原红线其余部分继续生效。

---

<a id="d-020"></a>
## D-020 · 新增第4数据组「业绩基准代理」；官方净值加主备（指数仍单源）；标签改源名称

**状态**：生效中（D 节与 B' 节已由 [D-022](#d-022) 取代）
**日期**：2026-07-21
**关联**：[D-015](#d-015)（估算全源死亡）/[D-013](#d-013)（直连模式）/[D-019](#d-019)（收益日期闸门）

### 背景

1. **估算源全死（D-015）后，看板"最新收益"功能失能**：盘中 `calcTodayProfit` 无任何今日数据可参与计算，所有基金被 D-019 日期闸门拦下，顶部显示 `-`。
2. **直连模式全部数据走腾讯单域名**：指数和官方净值都在 `qt.gtimg.cn` 上，单点故障会导致看板完全失明。
3. **用户有完整的季度产品审计习惯**，每只基金有自定义基准（`4_数据_产品知识库.md` A1.1），可作为日内方向代理。

### 决策

**A. 新增第4逻辑数据组——业绩基准代理**

- 定位：内部计算层，不依赖任何外部数据源。输入 = 产品库基准权重（`config.js BENCHMARK_PROXY`）+ 实时指数数据（来自数据组①）。输出 = 每只基金的估算日内变动（`estPct`/`estVal`）。
- 收益数据源通过 `DATA_SOURCE_SWITCH` 开关在"估算（estimate）"与"基准代理（benchmark）"之间二选一。当前设为 `"benchmark"`。
- **恢复路径（估算源将来复活时怎么办）**：
  - **通常什么都不用做。** `applyProxyEstimates` 的跳过条件是「当日已有**真**估算」（`estSource !== "proxy"`），
    所以是**逐只让路**：哪只先拿到真估算哪只先用，不必等全部复活、也不必手动切开关。
    且 `fetchEstimates` 在直连模式下 `needEstimate` 恒为 `true`，一直在问，源一活数据立刻进管道。
  - **换开关是总闸**，只在想彻底停用代理时才动（例如怀疑 `BENCHMARK_PROXY` 权重算错）。
    改成 `"estimate"` 后全站只有两个判断点让路（`applyProxyEstimates` / `reapplyProxyEstimates`，
    均为开头 early return），下游一个字不用改——它们只认 `estVal`，不关心谁填的。
  - **这是刻意的设计目标**（用户原话："下次有盘中估算了，直接开关一换，什么都不用动"）。
    新增代理相关逻辑时务必守住：**不许在下游任何地方判断 `estSource === "proxy"` 来分叉行为**，
    否则恢复路径就从"零改动"退化成"逐处排查"。唯一的例外是「已更新」徽标（D-022 E 节），
    那里判 proxy 是为了不说谎，不是为了改取值。
- 权重表字段名为 `legs` 而非 `equity`：混合品种只挂权益腿（债券+现金那 75-85% 日波动 1-2bp，摊到净值 <0.02%，建模没有收益）；**纯债品种整只挂国债指数腿**（兴全稳泰 A = 000012 × 100%）。一张表里两种腿共存，叫 `equity` 会让后来人以为纯债那行填错了。
- 纯债挂国债指数是**方向占位，不是收益预测**：国债指数久期与本基金不一致，量级对（日内 ±0.01% 上下）即可。原设计留空按"日收益≈0"处理，但空值在看板上渲染成 `--` 是个洞——挂个数量级正确的数比留白诚实，也比编个 0.00 诚实。
- 不符合基准代理条件的产品（纯债/退役/备选池）不参与，其收益照旧置空。
- 全天时段有效（与实测数据无关），仅非交易日退回到原有回退逻辑。

**B. 直连模式官方净值加主备；指数维持单源**

- 官方净值组：主 = 东方财富（`FundMNFInfo`），备 = 腾讯（`jj{code}`）。两源全挂则返回 unavailable，触发既有的 LKG 快照回退。**实测可用。**
- 指数组：**单源腾讯**（`qt.gtimg.cn`）。原方案要接新浪备源，实现后实测不可用（见「代价」栏），代码已删。
- 均为 direct 模式分支内扩展，不触及 gateway 模式，不破坏 DATA_MODE 双模架构。

**B'. 指数到货必须重画基金视图（竞态修复）** ⚠️ *实现已由 [D-022](#d-022) F 节取代（改调 `reapplyProxyEstimates`），但下面那条教训仍然成立*

- `main.js` 的 `INDICES` 频道除 `UI_updateIndices` 外追加 `UI_updateFunds`（仅 benchmark 开关下）。
- **这不是优化，是 D-020 自己制造的必修项**：本条之前基金视图对指数零依赖，两条频道互不相干；
  引入基准代理后「盘中估算」列与顶部「最新收益」的输入都是实时指数，而净值先于指数到货的那一轮，
  基金视图会拿空指数渲染一次，此后再没有任何事件叫它重画——顶部永久卡在 `-`。
  2026-07-21 实测复现：手动补一次 `UI_updateFunds()` 数字才出来。
  **教训：给某个视图新增跨频道数据依赖时，必须同时接上那条频道的重画线。**

**C. 标签文案改源名称，且主源也出声**

- 指数卡片和基金卡片的标签：不再显示"备用"，改为源名称（"腾讯"/"东财"/"新浪"）。
- **主源同样显示，不再静默**。源分离后"这个数字打哪来"不再是常识：腾讯与新浪两条链能力不同（新浪无 PE、无总市值，PE 主路会静默退回昨收），若主源静默，切源就无从察觉，只能靠数字变化反推。
- **标签每列只出现一次，挂在表头**（`<th>盘中估算</th>` / `<th>官方数据</th>` 内的 `thEstSrc` / `thOffSrc`），不在每行每卡重复。同一列所有基金必然同源（`fetchOfficialData` 整组取源，见 [D-002](#d-002)），逐行重复是纯噪音。卡片视图不挂标签，与原方案「收窄模式隐藏标签」一致。
- **做成实心小胶囊，不用描边字**：9px 的浅色文字在浅色底上几乎不可读（第一版实测如此）。文字色取 `--bg2`（页面底色）、底色取 `--t3`，浅色主题下自动是白字灰底、深色主题下是深字灰底，两边都对底色形成最大反差，无需写死颜色、不违反 3.5「颜色全走变量」。
- 配色分三级：主源（腾讯/东财）灰底 `--t3`；**备源与代理**（新浪/备用/proxy）加 `.is-alt` 换告警底 `--warn`；陈旧用 `--bg4` 底配 `--t2` 字——**告警色不能是常态**，否则失去提示意义。代理长期挂告警色是有意的：它本就该被替换掉，不该让人看惯。
- 中证全债（H11001）无实时行情，债券部分以国债指数（000012）代理，但不独立显示在标签上。
- 指数卡片标签从全行 `::before` 伪元素改为最后一张卡片右上角 `absolute` 定位，不占布局行。
  CSS 选择器写作 `.idx-cell .idx-src-tag` 而非 `.idx-src-tag`——后者与文件下方的 `.src-tag` 基础规则同权重且位置更靠前，会被整条盖掉（颜色与字号全部失效）。该规则只覆盖定位，配色与胶囊底沿用 `.src-tag`，两处标签视觉必须一致。

**D. 基准代理回填「盘中估算」列** ⚠️ *本节的"回填在 ui 层"与"顶部另走内部代理分支"已由 [D-022](#d-022) 取代，改为入库前统一回填*

- 代理值不止用于顶部「最新收益」，同时回填每只基金的 `estVal` / `estPct`，`estSource` 标为 `"proxy"`。否则该列在估算源全死后永远是 `--`，日内观察通道仍然缺失。
- **`"proxy"` 在表头出标签「代理」，但只在表头**（`srcTag` 的 `sourceLabels` 含该项，加 `.is-alt` 告警底）。
  演进过程值得记下，三步都是同一个判据在不同粒度上的答案：
  1. 初版在卡片、表格、顶部三处都标「基准估算」→ 用户判为画蛇添足。**对**：那一列本来就叫「盘中估算」，
     逐条重复既啰嗦又没增量信息。
  2. 于是全面撤掉，proxy 不出任何标签 → 估算列表头恒为空。**过头了**：分不清"还在用代理"
     和"源恢复了"，而估算源复活是个**没有通知的事件**，全靠这一格变字来发现。
  3. 定稿：表头出一次，写「代理」，用 `.is-alt` 告警底与真实源（灰底）区分。恢复时同时变字又变色。
  判据是**这个标签是否承载了别处拿不到的信息**——逐条重复没有，表头唯一一处有。
- `calcTodayProfit` 一度为标签返回 `usesProxy`，撤掉后无消费方，按红线 #8 删除，未留死变量。
- 回填由 `applyProxyEstimates` 完成，**放在 engine 层**：它是纯函数（`results, todayStr, indicesMap, quoteAt` 全部参数传入，不读 store 不碰 DOM），engine 正是纯函数层。初版误放 ui 层并因此与「ui 层禁止业务计算」冲突，一度考虑给它开白名单例外——**那是错的解法，正确解法是把函数放对地方**。
- **返回新数组新对象，绝不改 `results` 本体**——results 由 store 持有，就地写会顺着 `setLastResults` 沉进估算缓存，让代理值伪装成真估算被后续逻辑当作外部数据信任，且不可逆。
- 已有今日真估算时不覆盖：真数据永远优先于代理。
- 顶部收益不复用这份回填，仍走 `calcTodayProfit` 内部的代理分支。两者口径同源的前提是**调用方给两边传同一份 `indicesMap` 快照**——`UI_updateFunds` 里各调一次 `getIndices()` 之间若发生刷新，同屏两个数字就会各算各的。

### 理由

- **基准代理而非持仓穿透**：持仓穿透需维护持仓数据管道（季报抓取→个股行情×权重），工程量超出当前项目体量且精度受限于季报滞后。基准代理用产品库已有数据，零运维成本，方向准确率已满足"看方向"目的。
- **源分离而非搬进网关**：网关模式自带多源切换（D-001 KV LKG），但用户选择本次只做 direct 模式。直连加主备是直连例外的自然扩展（与 D-013 同款逻辑），不新增全局常量开关。
- **源标签而非"备用"**：看到"腾讯/东财/新浪"比看到"备用"更多信息量——用户能直接判断数据来源的可靠性。

### 代价

- **代码量：`js/` 净增 209 行、含 css/html 共 249 行**（复查后实测；初版风险表声称"总量持平"，
  不成立，已按红线 #8 如实报数）。换来的能力：基准代理估值链、东财净值主源、表头源标签。
  复查中已回收的膨胀：删死掉的新浪备源（-74）、删 `SYS_CONFIG` 聚合并把 30 处调用点改为裸常量（-约 25）、
  删无消费方的 `usesProxy`。
- `js/config.js` 重组为 10 个逻辑块（原扁平结构），新增 `BENCHMARK_PROXY` / `DATA_SOURCE_SWITCH` / `EM_BASE`。
  **`SYS_CONFIG` 已删**：重组初版同时保留裸常量与 `SYS_CONFIG` 聚合两条访问路径，
  19 个裸常量的唯一引用就是那个聚合对象，等于每个业务数字有两个名字，与红线 #4 用意相悖。
  复查时统一改为裸常量、删除聚合。分节注释（§1–§10）保留——那是可读性收益，与重复无关。
- 中证转债在新浪缺失（6/10 起停更）→ 若将来恢复新浪备源，鹏华丰利的基准代理在备源期间不可用。可转债权重 15% 且实际仓位已降至 5.06%（26Q1），实务影响有限。
- ⚠️ **新浪备源实测不可用，代码已删**：`hq.sinajs.cn` 不返回 CORS 响应头，浏览器 fetch 直接
  `Failed to fetch`；代码里想靠 `Referer` 头绕过它的防盗链也没用——`Referer` / `User-Agent`
  都是 fetch 的**禁止修改头**，浏览器静默丢弃。故 `_fetchIndexGroupSina` 恒返回 `null`。
  2026-07-21 实测确认后按红线 #8 删除（僵尸函数），解析逻辑可从本次提交前的 git 历史取回。
  **指数目前是单源，文档任何地方都不得再写"指数有主备"。** 要真正做出主备只有两条路：
  搬回网关代理（服务端无 CORS 限制），或换一个带 CORS 的行情源。
  **教训：接新数据源前先在浏览器里验一次 CORS，别等实现完才发现整条链是死的。**
- `BENCHMARK_PROXY` 需与产品库 A1.1 保持同步。每次季报审计后按产品知识和实际敞口手工校核。

### 代码位置

- `js/config.js`：§2 `DATA_SOURCE_SWITCH`，§5 `BENCHMARK_PROXY`（字段 `legs`），`EM_BASE`；删 `SYS_CONFIG`
- `js/data.js`：`_fetchEastmoneyOfficial`，`_fetchIndexGroup`/`fetchOfficialData` 调度
- `js/engine.js`：`getBenchmarkProxyPct`，`applyProxyEstimates`，`calcTodayProfit` 新增 `indicesMap` 参数
- `js/main.js`：`INDICES` 频道追加 `UI_updateFunds`（竞态修复，见 B'）
- `index.html`：表头 `thEstSrc` / `thOffSrc` 两个注入点
- `js/ui.js`：`srcTag` 源名称与 `is-alt` 分级，`renderSourceTags` 表头注入，`UI_updateFunds` 取单份指数快照并分发
- `js/ui-pe.js`：`renderIndices` 源标签定位与主源出声，`updatePeBar` 的 `_peDOM.display.innerHTML` 赋值
- `css/style.css`：移除 `idx-bar[data-status]::before`，新增 `.idx-cell .idx-src-tag` 与 `.src-tag.is-alt`；`.src-tag` 由描边字改实心胶囊（`--bg2` 字 / `--t3` 底）
- `js/interact.js`：`profitMap` 同步基准代理

### 须同步修订的正文章节

- `docs/02-系统架构.md`：§2.1 数据源描述由"三端点"更新为"四组数据"；§2.6 增加 direct 模式的备用源说明
- `CLAUDE.md` §4「数据源双模」：原文写"direct 时直连 `qt.gtimg.cn` 单请求覆盖官方净值+盘中估算两链"，
  净值主源改东财后该句失真，须改写并补基准代理段（**本条初版漏列 CLAUDE.md，2026-07-21 复查时补记**——
  正是 §3.1 警告的那种"决策写了、正文烂着"）
- `docs/03-实现约束.md` §3.6：追加 `applyProxyEstimates` 返回新对象的约束（反直觉设计，按 §3.3 双写）
- `docs/01-业务意图.md`：（无直接涉及，不修订）

### 取代关系

不取代任何已有决策。D-013 直连模式描述由"单源、无主备"更新为"指数主腾讯备新浪、净值主东财备腾讯"，非决策替换，系同一条的扩展。

---

<a id="d-019"></a>
## D-019 · 收益口径加日期闸门，「今日收益」更名「最新收益」；红线 #6 改为两类口径

**状态**：生效中
**日期**：2026-07-21
**关联**：[D-010](#d-010)（徽标与净值判据拆分，本条收紧其"数值可放宽"那半）、[D-015](#d-015)（估算断供）、[D-018](#d-018)

### 背景

用户 2026-07-21 盘中报告：估算数据为空，顶部却显示「今日收益 +160.13」。

复现（今天周二 07-21，官方停在 07-20，估算源断供）：

| 场景 | 官方 | 估算 | 显示 | 实际是什么 |
| --- | --- | --- | --- | --- |
| 正常盘中 | 07-20 | **07-21** | +90.00 | 今天的 ✓ |
| **估算断供** | 07-20 | 07-20 或无 | **+1010.00** | **周一的收益** ✗ |
| 盘后官方已出 | **07-21** | 07-21 | +190.00 | 今天的 ✓ |

**根因**：`calcTodayProfit` 取 nav/pct 的判据是 `f.offVal && (!estD || offD >= estD)`，
**整条路径没有任何"这数据是不是今天的"闸门**。估算缺席时它就退到官方，而盘中官方永远是昨天的，
于是把上一交易日的涨跌当成今日收益显示出来。

`isFundUpdated`（[D-010](#d-010) 拆出的徽标判据）此时是灭的，**徽标诚实、数字不诚实**——
这比两者都错更糟，因为红色大字比灰色小徽标显眼得多，用户先看到的是错的那个。

**这个 bug 早于 D-018，2026-07-21 的全量审查逐行读过 `calcTodayProfit` 却没发现**：
当时注意力全在 D-010 的徽标拆分上，默认"数值那条路已经被 D-010 审过了"，
没有独立验证它。**审查时对已有决策背书的代码要格外小心——决策记的是当初的意图，不是现在的行为。**

### 决策

1. **收益口径加日期闸门**：只有 `offD === today` 或 `estD === today` 的基金才参与收益计算；
   两者皆非今日则该只不参与。同为今日时**官方优先于估算**。
2. **无今日数据时的显示**，按市场状态分两种：
   - `WEEKEND` / `BEFORE_PRE`（今天本就不该有数据）→ 回退到最近可得数据，照常显示。
   - 其余时段（今天该有却没有，是异常）→ 显示 `-`，复用既有的 `isWaitingForOpen` 分支。
3. **「今日收益」更名「最新收益」**（`index.html` 的 `今日涨跌` 同步改）。非交易日显示的是上一交易日的数字，
   叫「今日」名不副实；「最新」在所有时段都准确，且不需要按状态切换标题。
4. **红线 #6 由「三处逐字一致」改为「两类口径」**（见下）。

### 理由

- **为什么闸门放在"是不是今天"而不是"有没有估算"**：用户原话是「有实时估算数据的才参与计算，
  官方优先于估算」。但盘后官方出来时估算也在，此时该用官方；而盘中官方永远是昨天的，该用估算。
  两种情况的共同点不是"有没有估算"，是**"这份数据是不是今天的"**。按日期闸门，两种情况自然都对。
- **为什么必须同时改抽屉的 profitMap**：若只改顶部，就会精确复现 [D-010](#d-010) 当初修掉的那个
  bug——「顶部今日涨跌归零，抽屉里却照常有收益，两者对不上」。**顶部和抽屉是同一个口径的两个出口，
  必须同步。**
- **为什么 `getNavByCode` 不加闸门**：它服务的是**市值**（持仓值多少钱、权益百分比），
  该用最新可得净值，哪天的都对。加闸门反而会让持仓总额在盘中归零。
  **这就是为什么"三处逐字一致"这条红线从一开始就不该是三处——它把两种口径捆在了一起。**
- **为什么非交易日不显示 `-`**：周末看最近一个交易日的结果是有价值的，且周末没有"今天该有数据"
  这回事，不构成异常。改名之后也不再有名实不符的问题。
- **为什么不做节假日日历**：`getMarketState` 只判周末，不判法定节假日。国庆等长假期间会被当作交易日，
  因而显示 `-` 而非最近收益。**已知限制，记入 05，不硬造日历**——维护一份年年要更新的节假日表，
  代价大于它解决的问题。

### 代价

- 红线 #6 的表述必须改，CLAUDE.md 与 `docs/03` §3.3 同步。**这是本次唯一动到红线的改动。**
- 法定节假日期间「最新收益」显示 `-`（见上，已知限制）。
- 盘中估算断供时用户看不到任何收益数字。**这正是目的**——没有今天的数据就不编今天的数字，
  与 [D-001](#d-001)、[D-018](#d-018) 同向。

### 代码位置

- `js/engine.js` `calcTodayProfit`：日期闸门 + 非交易日回退
- `js/interact.js` `openHoldingDrawer` 的 `profitMap`：同口径同步改
- `js/data.js` `getNavByCode`：**不动**（市值口径）
- `index.html`：`今日收益` / `今日涨跌` → `最新收益`
- `js/ui-holding.js`：抽屉表头 `今日收益(¥)` → `最新收益(¥)`

### 须同步修订的正文章节

- `CLAUDE.md` 红线 #6（三处逐字一致 → 两类口径）
- `docs/03-实现约束.md` §3.3（净值判据表拆成市值/收益两类）
- `docs/05-未实现项.md`（法定节假日日历，已知不做）

### 取代关系

不取代 [D-010](#d-010)——徽标与数值分家这个结构仍然成立，且本条正是靠它才定位到问题。
**收紧的是 D-010「数值可以放宽（fallback）」那半句**：fallback 的范围从"任意可得数据"
缩小为"今日数据，官方优先"。D-010 状态保持「生效中」。

---

<a id="d-018"></a>
## D-018 · 估算缓存按数据自带时间戳标记陈旧；Gist 推送时机与读侧节流修正

**状态**：生效中
**日期**：2026-07-21
**关联**：[D-014](#d-014)（三层回退，本条修正其实现）、[D-001](#d-001)（陈旧必须可见）、[D-015](#d-015)

### 背景

D-014 定的三层回退（fresh → localStorage → Gist → unavailable）**结构正确**，但实现有三处与其描述不符，2026-07-21 全量审查发现：

1. **陈旧标记根本不存在。** D-015 的理由栏白纸黑字写着「估算列会显示陈旧值（打"陈旧"标记）」，
   但 `js/data.js` 回退时返回 `source:"cached"/"gist"`，而 `js/ui.js` 的 `srcTag()` 只认
   `"backup"/"stale"`，这两个值落进去返回空串。**用户看到的是：缓存的旧估算与实时数据外观完全一致，
   零提示。** 这正是 [D-001](#d-001) 事故文档所禁止的「静默沿用」——上次是错误清空，这次是无声沿用，
   同一条原则的两个反面。
2. **Gist 兜底会把旧日期数据盖成今天。** `_lsSaveEstCache` 无条件写 `date: todayDateStr()`，
   而 Gist 取回路径直接调它落盘 → 一份隔周的快照被标成「今日数据」，TTL 从 2h 变 18h，
   配合第 1 点用户完全无从察觉。
3. **推的是当日第一笔，不是收盘快照。** `_maybePushEstToGist` 挂在每次成功之后，且当日标记
   **先于**推送写入 → 实际推的是 09:31 开盘估算，且推送失败当天不再重试。D-014 原意是「收盘后推送一次」。

**连带发现**：因第 3 点，`fm_est.json` 从未被真正写入 → 每次刷新都走 Gist 分支 → 404 → 异常被吞。
**每 60 秒一次带 token 的 GitHub 认证请求，全部无效**，且串在 `refreshData` 的 await 链上。
D-015 记的「白等一次超时」只覆盖了行情请求，没算这一条。

### 决策

1. 缓存结构去掉 `date` 字段，只留 `{ ts, data }`；`ts` 仅作硬上限（`EST_CACHE_MAX_AGE`，7 天）整份丢弃。
2. **新旧判断改由每个条目自带的 `estimateAt` 决定**。`srcTag` 扩展为按数据日期打标签：
   `estimateAt` 的日期 ≠ 今天 → 「陈旧」；是今天 → 不打（那确实是今日最后一笔，且时间已显示在旁边）。
3. Gist 推送时机移出 data 层，由 `js/interact.js` 的 `refreshData` 在 `mkt === "POST_MARKET"` 时触发；
   当日标记改为**推送成功后**才落。
4. Gist 读侧加节流（`EST_GIST_READ_THROTTLE`，30 分钟），**无论成败都推进时间戳**（失败即负缓存）。
5. 缓存读写移入 store 封装，走既有的 `_lsGet`/`_lsSet` 安全外壳。

### 理由

- **为什么按数据自带时间戳、而不按来源打标签**：来源只说明「这次是从哪拿的」，说明不了「数据本身多旧」。
  按来源打，收盘后沿用当日 14:59 那笔也会被标成陈旧（不准确）；按数据日期打，标签永远反映真实新旧，
  **并且顺带让第 2 点那个 bug 无处可藏——没有 `date` 字段可以被盖。** 修根源而不是加判断。
- **为什么推送时机放 interact 而不是 data**：`getMarketState` 属 engine(4)，而 data(3) 与 engine
  **平级互不依赖**（[02-系统架构.md](02-系统架构.md) §2.2）。interact(6) 是唯一能同时看到两边的合规位置，
  也让 data 层回归「只管取数与缓存」。
- **为什么收盘后第一个 tick 就是对的时机**：`refreshData` 每 60 秒一次，收盘后首个 tick 落在
  15:00–15:01，此时缓存里正是当日最后一笔（14:59）——既满足「收盘后」，也满足「最新」。
- **为什么读侧失败也要推进时间戳**：`fm_est.json` 可能长期不存在（估算源断供时永远推不上去）。
  只在成功时节流等于没节流。
- **D-014 状态保持「生效中」**：三层回退结构没有被推翻，本条修的是实现与其描述不符之处，
  不是决策替换。按只增不改的规矩，不动 D-014 原文。

### 代价

- `js/config.js` 增三个常量，`js/store.js` 增两个封装，`js/data.js` 净减（删三个私有函数 + 一个死变量）。
- 收盘前若浏览器一直没打开，当天不推 Gist。可接受：Gist 只服务「换设备冷启动」，
  而盘中冷启动本来就能直接取到 fresh 数据。

### 代码位置

- `js/config.js`：`STORE_EST_GIST_DATE`、`EST_CACHE_MAX_AGE`、`EST_GIST_READ_THROTTLE`
- `js/store.js`：`saveEstCache` / `loadEstCache`
- `js/data.js`：删 `_lsEstKey` / `_lsSaveEstCache` / `_lsLoadEstCache` / `_estGistPushedDate`；
  `_maybePushEstToGist` → `pushEstToGist`；`fetchEstimates` 读侧节流
- `js/ui.js`：`srcTag(source, dataDate, today)`；调用点在 `buildCardInnerHtml`
  **与 `buildTableInnerHtml`**——后者原本从不调 `srcTag`，导致桌面表格视图连既有的「备用」
  标签都从未出现过。只补卡片视图等于在 PC 上仍然静默沿用，D-018 的可见性承诺会架空一半。
- `js/interact.js`：`refreshData` 的 `POST_MARKET` 推送门

### 须同步修订的正文章节

- `docs/03-实现约束.md` §3.3（估算持久化段）
- `docs/02-系统架构.md` §2.4（直连模式估算说明）
- `docs/05-未实现项.md`（D-015 条下补指向）

### 取代关系

不取代 [D-014](#d-014)（其三层结构仍生效），只修正其实现。与 [D-001](#d-001) 同向——
**「不可用」和「静默沿用」都必须是对用户可见的状态。**

---

<a id="d-017"></a>
## D-017 · PE 锚定路径由 `PE_ANCHOR` 统一，持仓抽屉与 PE 栏对齐

**状态**：生效中
**日期**：2026-07-21
**关联**：[D-006](#d-006)（1.0/2.0 精度实测）、[D-013](#d-013)（`DATA_MODE` 同款常量开关惯例）

### 背景

`isEquityWrongDir`（方向异常判断）在两处用了**不同的 PE 源**：

| 位置 | 传入的 PE | 路径 |
| --- | --- | --- |
| `js/ui-pe.js` `updatePeBar` | `getEnginePE1` | 1.0 总市值路 |
| `js/interact.js` `openHoldingDrawer` → `js/ui-holding.js` | `getEnginePE` | 2.0 点位路 |

而 `js/engine.js` 注释与 [02-系统架构.md](02-系统架构.md) §2.6 都写明 1.0 是「PE 主路径
（大号 PE / bar / 触发状态 / **权益判断**）」。[D-006](#d-006) 实测：1.0 平均绝对误差 0.41pp / 最大 0.86pp；
2.0 平均 1.31pp / 最大 3.69pp。

**用户会看到**：PE 栏的权益数字染成正常色（1.0 判定不异常），点开持仓抽屉，同一时刻同一份持仓却染成
`--warn` 橙色（2.0 判定异常）——或者反过来。`PE_HIGH_THRESHOLD` 是 65，实时 PE 越贴近这条线越容易撞上。

### 决策

新增 config 常量 `PE_ANCHOR`（默认 `"mcap"`），engine 新增 `getAnchorPE` / `getRefPE` 两个分发纯函数，
**全站 PE 锚定收口到 `getAnchorPE`**；PE 栏小字改用 `getRefPE`，永远展示另一条路。

### 理由

- **为什么不直接把抽屉那处改成 `getEnginePE1` 了事**：那能修掉当下的 bug，但「哪条是主路径」这个知识
  仍然只散落在注释里，下次新增调用点还会再分叉一次。收口到一个常量 + 一个分发函数，
  **分叉在结构上就不可能再发生**。
- **为什么保留可切换而不写死 `"mcap"`**：[D-006](#d-006) 明确记了 2.0 的保留理由——
  「成分调整日总市值会跳变，那种日子点位路反而是有效参照」。写死等于删掉这个逃生口。
- **为什么沿用常量开关这个形态**：`DATA_MODE`（[D-013](#d-013)）已经是同一套惯例——一个常量、
  全站切换、改动收口在分发点。**项目里已有的习惯优先于新发明。**
- **为什么小字改成「另一条路」而不是固定 2.0**：这样 `PE_ANCHOR` 翻转时主/副自动对调，
  永远同时看得见两条路，开关才真正可用。

### 代价

- 净增约 12 行（常量 + 两个分发函数 + 注释），**违反红线 #8「改后代码量不许增加」的字面**。
  红线本意是禁止「包裹补丁绕开根源」，本条是收口而非包裹——如实记录，不掩饰。
- CSS 类名 `pe-bypass2` 在 `PE_ANCHOR="price"` 时语义反转（小字变成 1.0）。纯命名问题，
  本轮保留类名并在代码注释里点明，不动 CSS。

### 代码位置

- `js/config.js`：`PE_ANCHOR`
- `js/engine.js`：`getAnchorPE` / `getRefPE`
- 调用点：`js/ui-pe.js` `updatePeBar`、`js/interact.js` `liveUpdateHoldingPlan` / `openHoldingDrawer`

### 须同步修订的正文章节

- `docs/02-系统架构.md` §2.6（「PE bar 锚定 `getEnginePE1`」改为按 `PE_ANCHOR`）
- `docs/03-实现约束.md` §3.2（业务数字清单加 `PE_ANCHOR`）
- `CLAUDE.md` 第四节速览（与 `DATA_MODE` 并列提一句）

### 取代关系

无。补齐 [D-006](#d-006) 未覆盖的「1.0 / 2.0 各自用在哪」。

---

<a id="d-016"></a>
## D-016 · 追认：实时 PE 改由旁路引擎百分位提供；决策条目增「文档影响」栏

**状态**：生效中
**日期**：2026-07-21（**追认**，实际替换发生在 [D-006](#d-006) 前后）
**关联**：[D-006](#d-006)（拆双路验证层）、[D-017](#d-017)（锚定路径开关）

### 背景

2026-07-21 全量审查发现 `docs/01-业务意图.md` §1.2 / §1.7 与 `docs/03-实现约束.md` §3.2
描述的实时 PE 机制**在代码里已不存在**：

- **文档说**：用户夜间录入*收盘点位 / 增权点位 / 降权点位*，盘中用 `priceBuy / priceAnchor / priceSell`
  做**分段线性插值**反推实时 PE。
- **代码实际**：`getCurrentPE` 只从 `peData` 取 `bucketStr` 算缓冲带边界，PE 值直接取旁路引擎的百分位。
  `savePe` 只写 `bucketStr` 一个字段，`index.html` 的定档弹窗也只有 `peModalBucket` 一个输入框。
  `_currentIdxPrice` 参数在三处调用点全传 `null`（死参数），`peData.peYest` 回退分支永不可达。

替换本身合理且早已生效——旁路引擎（`automation/pe_nightly.py` → Gist `fm_pe_engine.json`）自动抓
乐咕沪深300滚动 PE，前端二分查全量历史 `peSorted` 得百分位，比人工录三个点位更准也更省事。
[D-006](#d-006)（2026-07-19）拆双路验证层时保留了引擎，条目写得完整。

**问题不在替换，在于替换后正文没跟上，而且烂了至少两个月没被发现。**

### 决策

1. **追认现状**：实时 PE 的唯一来源是旁路引擎百分位，`peData` 只承载 `bucketStr`。
2. 重写 `01` §1.2 / §1.7、`03` §3.2，旧版按 CLAUDE.md §3.2 归档，**不原地删**。
3. **决策条目新增「须同步修订的正文章节」必填栏**，堵住本次暴露的缺口。

### 理由

- **为什么不是「代码错了、按文档改回去」**：CLAUDE.md §3 那条「发现代码与文档冲突时，默认代码是对的、
  文档是漏的」正为此而设，[D-001](#d-001) 的事故恰是反着来造成的。旁路引擎有 D-006 的实测支撑
  （1.0 平均误差 0.41pp），人工录点位那条老路没有任何数据支撑，也早已无人维护。
- **为什么要加那一栏**：这次**不是记性问题，是激励结构问题**。写新决策是「追加」——成本低、有仪式、
  CLAUDE.md 反复强调；回头修正文是「修改」——成本高、没有触发点、没人催。
  **机制在无意中奖励了追加、惩罚了维护。** 不改激励，同样的事必然复发。
- **为什么追认而不是当无事发生**：正文是「判断代码是不是有意设计的唯一依据」（CLAUDE.md 原话）。
  一篇过期的 §1.2 比没有更危险——它会让下一个 AI 理直气壮地去「修正」正确的代码。

### 代价

- 归档两份旧正文，`docs/archive/` 增两个文件。
- 今后每条决策多填一栏。**这是有意增加的摩擦**，目的就是让「改完代码不改文档」变得别扭。

### 代码位置

无代码改动（本条为追认）。相关代码：`js/engine.js` 的 `getCurrentPE` / `getEnginePE` / `getEnginePE1`、
`js/interact.js` 的 `confirmPe`、`automation/pe_nightly.py`。

### 须同步修订的正文章节

- `docs/01-业务意图.md` §1.2、§1.7（数据流图）
- `docs/03-实现约束.md` §3.2（`getCurrentPE` 条）
- `CLAUDE.md` §3.1（新增必填栏）
- `docs/DECISIONS.md` 表头（条目模板）

### 取代关系

无。**不取代** [D-006](#d-006)——D-006 记的是拆验证层，本条记的是「定锚录入 → 引擎百分位」
这次替换本身，以及它留下的文档缺口。

---

<a id="d-015"></a>
## D-015 · 估算数据源（天天基金/腾讯）持续失效，暂不处理，网关验收作废归档

**状态**：观察中
**日期**：2026-07-21
**关联**：[D-013](#d-013)（直连数据源）、[D-014](#d-014)（估算持久化三层回退）

### 背景

根目录 `PENDING_ACCEPTANCE.md` 原是"网关盘中验收"待办（唯一未完成项：验证腾讯估算备用、
东财备用指数延迟）。2026-07-21 盘中（09:35，交易日）重跑验收，结果比 07-20 的记录更差：

- **天天基金 `fundgz`（primary）**：`curl` 直接返回 `301 Moved Permanently`，接口已彻底下线。
- **腾讯 `qt.gtimg.cn`（backup）**：估算净值/估算涨跌幅两字段连续两个交易日盘中恒为
  `0.0000`，不是偶发抖动。
- 因估算腿失败，workflow 提前终止，验收清单第二项（东财备用指数延迟对比）连续两次未跑到，
  **始终未验证**。

联网核实：证监会证券基金机构监管司 2026-01-29 发布通报，要求代销及第三方平台于 1 月 30 日
前全面下架"基金实时估值"功能（误导投资者，2023 年已叫停过一次，此次系"死灰复燃"后从严
整改）。天天基金、腾讯理财通均在官方公告中确认了下架。**但用户反馈实际断供是从 2026-07-17
前后（上周四/五）才开始，此前一直正常**——时间线对不上 1 月的政策窗口，多轮联网搜索也没
找到 7 月中旬对应的后续通报、执行宽限期或平台公告，**断供的直接触发原因未能查实**，
不排除是 1 月政策的延迟落地/分批验收，但没有证据支撑，不写实锤结论。

### 决策

1. **不追查根因、不换估算源、不改代码**——`js/data.js`、`workers/fund-market-api/` 均不动。
   已核实现有代码对"估算整组不可用"已有意设计好兜底（见下），继续观察即可，没有紧急性。
2. **`PENDING_ACCEPTANCE.md` 归档删除**，即使验收清单两项都未真正跑通，也不再按原计划
   "跑通再收尾"——用户明确决定放弃这次验收，不再等外部数据源恢复。原文件全文移至
   `docs/archive/PENDING_ACCEPTANCE-2026-07-21.md`，含完整的失败记录、curl 复现步骤、
   政策调研过程，供以后需要时查证。
3. `docs/05-未实现项.md` 里指向该文件的条目一并更新，不再引用已删除的根目录文件。

### 理由

- **计算不受影响**：`js/engine.js` 的 `isOfficialUpdated` 判据（`f.offVal && (!estD || offD >= estD)`，
  三处逐字一致，见 [03-实现约束.md](03-实现约束.md)）本来就把"估算整组不可用"当成合法状态处理——
  没有估算就退回官方净值，且"已更新"徽标（`isFundUpdated`）会诚实地保持未点亮，不会让用户
  误以为今天已收盘核算完。这是代码注释里写明的有意设计，不是意外兼容。
- **不会污染已有数据**：[D-001](#d-001) 的网关 last-known-good 保护对此完全适用且未受影响——
  没有新数据就吐旧数据（标 `stale`），不会把已有数据冲空。指数、官方净值两组数据独立请求，
  未被估算失败牵连。
- **`DATA_MODE=direct`（D-013）下的用户体感**：估算列会显示陈旧值（打"陈旧"标记，见 D-014 的
  localStorage/Gist 回退）或空白 `--`，不会出现看似正常实则错误的当日涨跌数字；持仓收益要等
  晚上官方净值出来才更新，这段时间内的数字按昨天算，滞后但不错。
- **代价可接受**：每次盘中刷新仍会照常尝试打这两条注定失败的请求，白等一次超时，只浪费
  请求次数和等待时间，不报错、不崩溃、不影响其它数据类型。
- **不写实锤根因**：断供时间线（7月中旬）与查到的监管政策时间线（1月底）对不上，为避免像
  [D-001](#d-001) 事故那样把未经证实的推断当成事实写进文档，本条只记录"已核实的现象"和
  "查过但没找到直接证据"，不认领因果关系。

### 代价

- 验收清单两项（腾讯估算备用是否可用、东财备用指数是否延迟）**永久悬而未决**，
  `docs/03-实现约束.md:59`"所有品种（含纯债）正常情况下都有盘中估算数据"这条描述
  与当前实况不符，暂未同步修订（估算源随时可能恢复，等观察明朗再一并处理，避免来回改）。
- 若后续要换估算源或调整降级策略，是新的业务决策，不在本条覆盖范围内，需另开条目。

### 代码位置

无代码改动。归档文件：`docs/archive/PENDING_ACCEPTANCE-2026-07-21.md`。

---

<a id="d-014"></a>
## D-014 · 估算数据持久化：localStorage + Gist 三层回退

**状态**：生效中
**日期**：2026-07-20
**关联**：[D-012](#d-012)（直连模式例外）、[D-013](#d-013)（直连数据源）

### 背景

直连模式（D-013）下，腾讯行情盘后估算字段返回 0 → `_parseTencentFunds` 的 `estimateNav > 0` 过滤
→ 估值列全部变成 `"--"`。D-012 为了网关性能加了盘后跳过估算的优化，但在直连模式不需要
（与官方链共享 in-flight 去重，无额外网络代价），反而造成了"收盘=空白"的体验问题。

更严重的是：即使当时显示了，换设备或清缓存后也全部丢失。in-memory 变量不能跨设备持久化。

### 决策

估算数据做三层回退持久化：

```
fresh（腾讯直连有数据 → 返回）
  ↓ 空
localStorage（本地缓存 → 返回，标记来源 "cached"）
  ↓ 空
Gist fm_est.json（跨设备兜底 → 返回 + 同步到 localStorage，标记来源 "gist"）
  ↓ 空
unavailable（真正没数据）
```

### 理由

- **localStorage**：即时、无网络、解决同设备刷新/关机后恢复。当日数据 TTL 18h。
- **Gist**：复用现有 Gist 同步基础设施（`_cloudReadFile`/`_cloudWriteFile`），新文件 `fm_est.json`。
  收盘后推送一次（fire-and-forget，每天一次），解决换设备首次打开的问题。
- **代理模式不影响**：`DATA_MODE="gateway"` 时 `needEstimate` 策略原样保留。

### 代价

- `js/data.js` 新增约 50 行（三个缓存函数 + Gist 推送）。
- 新增 `GIST_FILE_EST = "fm_est.json"` 到 `config.js`，一个 localStorage key `STORE_EST_CACHE`。
- `interact.js` 的 `needEstimate` 加了一条 DATA_MODE 分支判断。

### 代码位置

- `js/config.js`：`GIST_FILE_EST`、`STORE_EST_CACHE`
- `js/data.js`：`_lsSaveEstCache` / `_lsLoadEstCache`、`_maybePushEstToGist`、`cloudReadEst` / `cloudUpdateEst`；
  `fetchEstimates` 直连模式改为 async，内联三层回退
- `js/interact.js`：`needEstimate` 在 `DATA_MODE="direct"` 时为 `true`（始终调 `fetchEstimates` 走缓存）

---

<a id="d-013"></a>
## D-013 · 数据源双模：`DATA_MODE` 切换网关 / 腾讯直连

**状态**：生效中
**日期**：2026-07-20
**取代**：[D-008](#d-008)（"前端一律不直连第三方行情"）—— 直连模式即前端直连腾讯，故 D-008 不再作为全局红线，降级为"网关模式下的约束"。

### 背景

原架构（D-008）前端只走 Cloudflare 网关 `API_BASE`，主备切换 + KV last-known-good 全在网关侧。
迁移后实测发现：**Cloudflare Worker 出站 IP 在境外，访问国内数据源天然慢 4–8 倍**
（东方官方 1.3s vs 直连 0.28s；腾讯行情 0.6s vs 直连 0.15s）。
而天天基金 `fundgz` 老接口已 301→404 死亡，东方 `FundMNFInfo` 前端直连被 `ErrCode:61136` 拦截，
**唯一还能前端直连的是腾讯 `qt.gtimg.cn`**（带 `Access-Control-Allow-Origin: *`，GBK 编码）。

用户怀念早期前端直抓的丝滑感，且明确"不强制主备"，故选定路线 A：前端直连腾讯。
同时为保留网关作为兜底，不删除 `workers/` 代码，而是用**一个开关**让两条链路并存。

### 决策

1. **`config.js` 加 `DATA_MODE` 常量**（`"gateway"` | `"direct"`），当前默认 `"gateway"`。
   改这一个常量 + 推一次即全站切换，不动任何业务代码。
2. **`DATA_MODE === "direct"` 时**：`js/data.js` 走 `_fetchTencentFunds` / `_fetchIndexGroupTencent`，
   浏览器直连 `TX_BASE`，字段产物与网关**逐字段对齐**，保证 `fetchSingleFund` / `setIndices` / `setQQIndex` 无感切换。
3. **`DATA_MODE === "gateway"` 时**：完全沿用既有网关路径，行为不变。
4. **`workers/` 网关代码原样保留**，不删。直连模式前端不请求它，仅作备选。

### 理由

- **为什么用运行时开关而非两条 Git 分支**：线上静态站只认一条分支，切分支要重新合并部署，
  且两套 `data.js` 长期并行必冲突。一个常量 + 一份代码同时装两套数据源，切换成本最低、回滚最稳。
- **为什么只用腾讯单源**：fundgz 已死、东方前端被 61136 拦，仅腾讯可直连。单源 = 无主备，
  用户已确认接受（直连模式无 last-known-good 兜底，断网即无数据；如需可后续加 localStorage 缓存）。
- **为什么字段要对齐网关**：渲染/计算层只认 `estimateNav/officialNav/f2/f3/...` 这套字段名，
  直连产物保持同构，下游零改动。

### 代价

- **代码量增加**：`js/data.js` 新增 TX 直连段（约 130 行），临时突破红线 #8"改后代码量不许增加"。
  该红线就本特性**临时放行**——双链路是用户明确要的功能，无法在不增代码的前提下实现。
- **直连模式无 LKG 兜底**：网关的 KV last-known-good 在直连模式失效；断网/腾讯故障 = 数据空白。
- **GBK 解码硬依赖**：必须用 `TextDecoder("gbk")`，浏览器原生支持；若某环境不支持该标签会乱码。
- **恒生指数字段差异**：`hkHSI` 布局比 A 股指数短（78 vs 88 字段），其 PE/市值/时间为 0/null，
  与网关同字段位置表现一致；显示与 PE 锚定（锚定沪深300）不受影响。

### 代码位置

- `js/config.js`：`DATA_MODE`、`TX_BASE`、`TX_INDEX_QQ`
- `js/data.js`：`_txNum` / `_txDate` / `_parseTxAssignments` / `_fetchTencentFunds` / `_parseTencentFunds` / `_fetchIndexGroupTencent`；
  `fetchOfficialData` / `fetchEstimates` / `_fetchIndexGroup` 三处按 `DATA_MODE` 分流
- `CLAUDE.md` 红线 #1 改为描述双模；`docs/02-系统架构.md` 2.4 节补双模说明

---

<a id="d-012"></a>
## D-012 · 盘后跳过估算请求，节省 4s+ 刷新等待

**状态**：生效中
**日期**：2026-07-20

### 背景

刷新时 `Promise.all([fetchOfficialData, fetchEstimates])` 串等两者。盘后（POST_MARKET）：
- 官方：1.3s 秒回（一次批量 API）
- 估算：4.3s 才回（`fundgz.1234567.com.cn` 对 CF Worker IP 限速 → 6 并发拖秒数 →
  超时回退备份 → 备份返回全 0 → 解析器拒掉 → `unavailable`）

盘后这 4 秒无商业意义——估算来源只在天盘中有波动，盘后必然 unavailable。

### 决策

在 `refreshData` 中加市场状态判断：只在 `PRE_MARKET / TRADING / MID_BREAK` 时段调 `fetchEstimates`，
其余用 `{ source: "unavailable", data: new Map() }` 秒回。

### 理由

- `POST_MARKET / BEFORE_PRE / WEEKEND` 时估算始终 unavailable，不值得调网关。
- 盘中刷新不受影响，继续走完整请求链路。
- 对 D-006（拆除 PE 双路验证）形成呼应：盘后数据稳定后，不需要再反复拉"不可能有的"盘中估算。

### 代码位置

`js/interact.js` `refreshData`

---

<a id="d-011"></a>
## D-011 · 网关 in-flight 请求去重

**状态**：生效中
**日期**：2026-07-20

### 背景

`router.mjs` 的 `cache` Map 只缓存"已完成结果"，不缓存"进行中的 Promise"。
同一 cacheKey 在 TTL 窗口内并发到达时，所有请求都各自调起 `selectGroup` → 打上游，
直到第一个完成写回缓存。触发条件窄（TTL 8s~60s 窗口内并发重叠），但一旦触发就是
无意义的重复上游调用。

### 决策

加 `inflight` Map，存储进行中的 Promise。请求到达时：
1. 有 inflight → 搭车等同一个 Promise
2. 有已完成缓存 → 秒回
3. 都没有 → 发起一趟，写进 inflight，`finally` 中释放

**`force` 参数跳过去重**（诊断用）。

### 理由

- 对单机自用影响极小，但对 PWA 公用部署或爬虫误触有防护价值。
- 改动 <15 行，风险低。
- Cloudflare Pages Functions 是 per-isolate，跨 isolate 不共享 inflight，
  同 isolate 内的幂等已是当前能达到的最大保护。

### 代码位置

`workers/fund-market-api/src/router.mjs`

---

<a id="d-010"></a>
## D-010 · "已更新"徽标与净值选取判据拆开

**状态**：生效中
**日期**：2026-07-20

### 背景

`calcTodayProfit` 中 `isOfficialUpdated` 身兼两职：
1. 决定 nav/pct 取哪边（官方还是估算）
2. 驱动 `allUpdated` → 顶部"已更新"徽标

2026-07-20 晚间盘中估算整组不可用（`status: "unavailable"`），`estD` 为空，
`!estD` 短路使整条判据退化为 `!!f.offVal`——任何有官方净值的基金都被判为"已更新"，
与 `offD` 是不是今天完全脱钩。用户看到部分基金官方还是昨天的，徽标却亮着。

### 决策

**净值选取口径（`isOfficialUpdated`）保持不变**：`mktState === "WEEKEND" || "BEFORE_PRE" || !!(offVal && (!estD || offD >= estD))`。
三处（`getNavByCode`、`openHoldingDrawer` 的 profitMap、`calcTodayProfit` 的 nav/pct 选取）维持一致。

**新增"徽标口径"（`isFundUpdated`）**，仅用于驱动 `allUpdated`：
```javascript
const isOffToday = offD === todayStr;
const isEstToday = estD === todayStr;
const isFundUpdated = isOffToday || isEstToday;
```

**两者分工**：`isOfficialUpdated` 管数值（放宽 fallback，避免整只跳过导致今日涨跌归零）；
`isFundUpdated` 管状态（严格，必须有今天的数据才算"已更新"）。

### 理由

- **数值可以放宽、状态必须严格**：估算空时用昨天官方凑出 0 涨跌是可接受的 fallback；
  用昨天数据显示"已更新"会误导用户，比显示空白更危险。
- **估算整组不可用是常态（盘后）**，不能因此把"放宽的 fallback"误用到状态判定上。

### 代码位置

`js/engine.js` `calcTodayProfit`

### 教训

**判定状态徽标 vs. 判定数值选取，是两件不同的事。** 未来加任何带"状态标签"的 UI 元素，
先问自己："这条展示信息的前提判据，和数值选取的判据，是同一道题吗？"

---

<a id="d-009"></a>
## D-009 · 上游请求一律带缓存击穿参数

**状态**：生效中
**日期**：2026-07-19

**决策**：`upstreams.mjs` 的 `fetchWithTimeout` 给每次上游请求 URL 追加时间戳参数，并传 `cf.cacheTtl:0`。

**理由**：`fundgz` 等上游前面挂着第三方 CDN，曾按出口 IP 把响应缓存住 **40+ 分钟**不更新，
导致看板显示长时间不变的陈旧估算。此举不影响 `router.mjs` 自己那层 15 秒节流缓存
（那层是上游限流器，不是缓存）。

---

<a id="d-007"></a>
## D-007 · 不再用 gitignore 藏"本地专属"文件

**状态**：生效中
**日期**：2026-07-16

**决策**：`CLAUDE.md` 与 Worker 触发器源码移出 `.gitignore`，纳入公开仓库正式追踪。

**理由**：gitignore 的文件**永远不会被 clone/pull/checkout 带回来**，只活在某一台机器的磁盘上。
本仓库已实际发生过丢失：本地工作目录建于 2026-07-09 的一次 clone，晚于两文件被移出追踪的时间点，
clone 时它们已不在可追踪历史里，靠 `git log --all -- <path>` 找 Delete commit 的父提交手工找回。
反复"考古恢复"的代价大于公开这两份文件的代价。已核对二者均无明文密钥。

**推论**：以后任何"本地专属、不想公开"的文件，优先选"仓库外单独备份"，而不是单纯 gitignore。

---

<a id="d-006"></a>
## D-006 · 拆除 PE 双路验证层，保留 2.0 展示

**状态**：生效中
**日期**：2026-07-19

**决策**：删除 16:00 快照、夜间与官方配对、`validation-log.json`、`RUN_ACTION=snapshot` 分支
及 Worker 的 `snapshot` 路由。**2.0 点位路保留在看板上。**

**理由**：验证层唯一目的是回答"1.0 和 2.0 哪个准"。15 个可比交易日（06-25～07-17）结论已足够确定：

| | 1.0 总市值路 | 2.0 点位路 |
| --- | --- | --- |
| 平均绝对误差 | 0.41pp | 1.31pp |
| 最大绝对误差 | 0.86pp | 3.69pp |
| 逐日更接近官方 | 14 / 15 | 1 / 15 |

保留 2.0 是因为成分调整日总市值会跳变，那种日子点位路反而是有效参照。

**代价**：Master-Scheduler 侧 16:00 那一跳需同步取消。若日后要重建验证，从 git 历史取回，
不要在引擎里留半套。

---

<a id="d-005"></a>
## D-005 · 配置同步用版本号"谁新谁赢 + 反向自愈"

**状态**：生效中
**日期**：2026-06

**决策**：低频配置（持仓/基金列表/降权预案/优先卖出）带版本号 `v`；
本地改动即自增，拉取时只采纳 `remoteV > localV`；若本地反而更新则延迟推回云端。
PE 定锚**不**版本化。

**理由**：配置录错或被旧端覆盖损失大，PE 丢了肉眼一秒能重录。
两类数据的更新频率与重要性不同，用一套机制会让高频的 PE 白白背上复杂度。
反向自愈是为了兜住"上次推送被系统杀掉"的情况，保证任何一次有效编辑都不会被回退。

**代价**：每个配置写入封装都必须自己调 `bumpConfigVer()`。
`saveSellPlan` 曾因"唯一调用点紧跟在 `saveHoldingsData` 之后"而漏掉，属随时会响的哑雷，2026-07-19 已补。

**代码位置**：`js/store.js` `bumpConfigVer` / `importSnapshot`；`js/interact.js` `syncCloud`

---

<a id="d-004"></a>
## D-004 · 增权预案 `totalFriction` 恒为 0

**状态**：生效中

**决策**：增权路径的摩擦成本恒为 0，不参与计算。

**理由**：增权的卖出端是纯债（系数 0）、买入端是 C 类（系数 1），两端都零摩擦。
`FEE`（原 `SYS_CONFIG.FEE`，D-020 后改裸常量）只对混合型（系数 `∉ {0,1}`）计提。

**⚠️ 看起来像忘了实现，是有意设计。**

**代码位置**：`js/engine.js` `calcBuyPlanDraft`

---

<a id="d-003"></a>
## D-003 · 增权份额除以兴全净值而非买入标的净值

**状态**：生效中

**决策**：`calcBuyPlanDraft` 里 `sharesA500C` / `sharesZZ500C` 除以兴全净值 `xqNav`，不是 `a500cNav`。

**理由**：业务关心的是"为各买入桶需要**卖出多少兴全份额**"，不是"买到手多少份额"。
用户的实际操作是先赎兴全、再申购，手里要的是赎回份额数。

**⚠️ 看起来像单位错误，是有意设计，勿"修正"。**

**代码位置**：`js/engine.js` `calcBuyPlanDraft`

---

<a id="d-002"></a>
## D-002 · 网关"整组成败"，禁止主备混用

**状态**：生效中（边界已由 D-001 收窄）
**日期**：2026-07-16

**决策**：一组请求要么整组来自主线路、要么整组来自备用线路，绝不逐只混用；凑不齐则该组不可用。

**理由**：主备两源的口径、时延、字段含义都不同。半组主源半组备源，用户看到的是一张
内部不自洽的表——两只基金的涨跌幅根本不可比。宁可整组不可用，也不给自相矛盾的数据。

**⚠️ 这条不等于"许可清空看板"。** "整组不可用"说的是**这一次请求的结果不可用**，
不是"把已有数据删掉"。不可用时应走 D-001 的陈旧回退。二者不冲突：
**D-002 管的是"不许混"，D-001 管的是"不许丢"。** 2026-07-20 的事故正是把前者误读成了后者的授权。

**代码位置**：`workers/fund-market-api/src/router.mjs` `selectGroup`

---

<a id="d-001"></a>
## D-001 · 行情数据 last-known-good 保护，落在网关侧

**状态**：生效中
**日期**：2026-06-19 确立（前端版）→ 2026-07-20 被拆除（事故）→ 2026-07-20 重建于网关侧

### 背景（含事故经过）

2026-06-19 的 `9a41e0e`（298 行，横跨 data/store/ui）确立了一条原则：
**行情数据拿不到新的，就继续用上次的好数据，并明确标记为陈旧**。当时的实现是
`setIndicesUnavailable()` → 回退 localStorage 快照 → `mode:"stale"` → UI 上 `is-stale` 灰化。

这条原则**从未写进任何文档**，只活在指数那条代码路径里。

2026-07-20 做网关迁移（`b76b928`）时，AI 读到文档里 2.6 节"任一组请求失败即整组降级为不可用"
和"`unavailable` 时官方字段保持空值"，照着执行，于是：

- `data.js` 官方组失败时 `delete officialBatchCache[cacheKey]`——**主动删掉**上次的好数据。
  旧实现 `if (result) offCache[code] = ...` 是失败**不写**缓存，下次 TTL 内继续返回上次好数据。
- 失败波及范围从"每只独立"（`funds.map(fetchSingleFund)`）变成"一只解不出、全组归零"
  （网关 `records.every(Boolean) ? records : null`）。
- `setLastResults(res)` 整体覆盖，没有指数那样的 stale 回退。

**用户看到的现象**：收盘后天天基金/腾讯都停供盘中估算，整组 unavailable，
一次 60 秒刷新就把一整天的估算数据冲成 `--`。

### 决策

1. **行情数据禁止因单次失败清空。** 拿不到新数据 → 返回上次好数据 + 标记陈旧。这是硬红线。
2. **保护落在网关侧，不落在浏览器。** 网关持久化最后一份完整好数据（KV），
   失败时以 `status:"stale"` 返回。前端只负责把陈旧态显示出来。
3. 前端原有的指数 localStorage 快照保留，作为"网关整个够不着"时的第二道防线。

### 理由

- **为什么不能只在前端**：localStorage 是**单机**的。换一台电脑、或手机冷启动，
  晚上打开看板照样是空白——保护等于没有。网关侧存一份，所有设备一次受益。
- **为什么是 KV 而不是 `router.mjs` 里那个 `cache` Map**：那个 Map 活在 isolate 内存里，
  冷启动即丢，跨 colo 不共享，撑不住"晚上打开还能看到当天数据"。
- **为什么必须标记陈旧而不是静默沿用**：看板是盯盘工具，用户据此判断要不要下单。
  把昨天的数当今天的显示，比显示空白更危险。**陈旧可以接受，陈旧且不告知不行。**

### 代价 / 已知副作用

- KV 有写入配额，需对写入节流（不是每次成功都写）。
- 陈旧数据的"保质期"需要定上限，跨交易日的旧数据不应继续冒充。
- `status` 多出 `stale` 一态，网关契约、前端 `_fetchGroup`、测试、README 都要跟着改。

### 代码位置

- 网关：`workers/fund-market-api/src/router.mjs`、`src/lkg.mjs`
- 前端消费：`js/data.js` `_fetchGroup`；前端指数第二道防线：`js/store.js` `setIndicesUnavailable`
- 陈旧态呈现约定：`js/ui-pe.js` `is-stale` / `js/ui.js` `.stale`

### 教训（这条比决策本身更重要）

**原则只写在代码里，等于没写。** 更糟的是，文档里当时还留着一句看起来相反的话（2.6 的"保持空值"），
于是 AI 越是老实照文档执行，越是精准地把保护拆掉。
`docs/` 的归档与本日志机制就是为堵这个洞而建，见 `CLAUDE.md` 第三节。
