# 第五篇 · 未实现项

> 本篇只登记已经识别、但尚未获批实施的能力。这里不是自动待办；没有用户裁决不得实现。

| 未实现项 | 当前行为 | 需要用户决定 |
|---|---|---|
| 中证500C 的 PE<45% 激活门槛 | A500C 触顶后的溢出资金直接由中证500品种承接 | 是否增加硬门槛；门槛未满足时资金留在哪里 |
| T+7 FIFO 成熟份额 | 看板只给目标份额，份额年龄由用户在交易端判断 | 是否记录每笔申购及赎回顺序 |
| 枷锁层硬排除 | 降权第三层按用户配置比例执行，可能包含枷锁层 | 是否在引擎层永久排除指定品种 |
| PE<35 破窗 SOP 与黑天鹅静默 | 仍由用户人工执行 | 资金来源顺序、静默时长及恢复条件 |
| 法定节假日日历 | `getMarketState` 只判周末；长假可能显示等待今日数据 | 是否接受年度日历维护成本或引入外部交易日历 |
| 折叠卡片数字与左色条口径 | 色条走 `getActivePct()`，数字走 `miniMode`；估算与官方反号时可能颜色不一致 | 统一跟随当前显示数字，还是固定跟随活动净值 |

以下不是未实现项，也不得从旧资料恢复：

- 人工录入夜间 PE 锚和漂移修正：已由自动夜间 PE 引擎取代。
- 公开第三方基金实时估值接口：当前产品明确使用基准代理，不等待旧接口恢复。
- `/v1/funds/estimate`、`fm_est.json`、估值 localStorage：已从正式架构移除。
- 为官方净值增加浏览器直连兜底：正式路径固定为采集器双源 → `NAV` KV → 网关读端点。
